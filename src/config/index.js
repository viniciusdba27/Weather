const baseUrl = "https://www.metaweather.com";
const ApiUrl = `${baseUrl}/api`;

const Api = {
  search: `${ApiUrl}/location/search/`,
  city: `${ApiUrl}/location/`,
  icon: `${baseUrl}/static/img/weather/png/64/`,
};

export default Api;
